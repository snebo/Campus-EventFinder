import { Routes } from '@angular/router';

import { ForgotPasswordPageComponent } from './forgot-password.component';

export const LOGIN_ROUTES: Routes = [{ path: '', component: ForgotPasswordPageComponent }];
